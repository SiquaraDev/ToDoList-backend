# Backend from [ToDoList-react-native](https://github.com/SiquaraDev/ToDoList-react-native)

## How To Use

clone this directory and:

- run `npm install` (install the dependencies)
- run `npm start` (run the nodemon server)
